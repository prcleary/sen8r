% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/view_senaite_output.R
\name{view_senaite_output}
\alias{view_senaite_output}
\title{View and Inspect SENAITE Output}
\usage{
view_senaite_output(x, new_tab = TRUE)
}
\arguments{
\item{x}{\code{list}. A (possibly nested) SENAITE API response object.
Typically a single item returned by \code{get_senaite_data()}.}

\item{new_tab}{\code{logical(1)}. If \code{TRUE} (default), opens the flattened
output in the RStudio Viewer using \code{View()}. If \code{FALSE}, the
tibble is returned invisibly without opening a viewer tab.}
}
\value{
Invisibly returns a \code{tibble} with two columns:
\itemize{
\item \code{name}: field names
\item \code{value}: corresponding values (after flattening with \code{unlist()})
}
}
\description{
Converts a nested SENAITE API response object into a flat two-column
tibble for easy inspection. Optionally opens the result in the RStudio
data viewer.
}
\details{
This function is primarily intended for exploratory analysis and
debugging of raw SENAITE API responses.

The function:
\enumerate{
\item Flattens the input object using \code{unlist()}.
\item Converts it to a two-column tibble using \code{tibble::enframe()}.
\item Optionally opens it in the RStudio Viewer.
}
\subsection{RStudio Requirement}{

The \code{View()} function is intended for interactive use in RStudio.
In non-interactive or non-RStudio environments, \code{new_tab = TRUE}
may have no effect.
}
}
\examples{
\dontrun{
# Retrieve one analysis request
ars <- get_senaite_data("analysisrequest", limit = 1)

# View raw output structure
view_senaite_output(ars[[1]])

# Get tibble without opening Viewer
output_tbl <- view_senaite_output(ars[[1]], new_tab = FALSE)
}

}
\seealso{
\code{\link[tibble]{enframe}},
\code{\link{get_senaite_data}}
}
