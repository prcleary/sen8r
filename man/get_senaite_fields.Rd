% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_senaite_fields.R
\name{get_senaite_fields}
\alias{get_senaite_fields}
\title{Retrieve Selected Fields from a SENAITE Endpoint}
\usage{
get_senaite_fields(
  endpoint,
  username = keyring::key_get("senaite_username"),
  password = keyring::key_get("senaite_password"),
  fields = "",
  params = list(complete = TRUE, children = TRUE),
  verbose = TRUE,
  flatten = TRUE,
  base_url = keyring::key_get("BASE_URL")
)
}
\arguments{
\item{endpoint}{\code{character(1)}. Name of the SENAITE API endpoint to query
(e.g., \code{"analysisrequest"}, \code{"client"}, \code{"worksheet"}).}

\item{username}{\code{character(1)}. SENAITE username for HTTP authentication.
Defaults to the value stored in the system keyring under
\code{"senaite_username"}.}

\item{password}{\code{character(1)}. SENAITE password for HTTP authentication.
Defaults to the value stored in the system keyring under
\code{"senaite_password"}.}

\item{fields}{\code{character}. Additional field names to retrieve from each
SENAITE object. Core fields (\code{"title"}, \code{"uid"}, \code{"parent_path"},
\code{"portal_type"}) are always included.}

\item{params}{\code{list}. Named list of query parameters passed to
\code{get_senaite_data()}. Defaults to
\code{list(complete = TRUE, children = TRUE)}.}

\item{verbose}{\code{logical(1)}. If \code{TRUE} (default), prints progress messages.}

\item{flatten}{\code{logical(1)}. If \code{TRUE} (default), the result is flattened
into a \code{data.table} using \code{flatten_senaite_output()}. If \code{FALSE},
a list of named lists is returned.}

\item{base_url}{\code{character(1)}. Base URL of the SENAITE instance.
Defaults to the value stored in the system keyring under \code{"BASE_URL"}.}
}
\value{
If \code{flatten = TRUE}, returns a \code{data.table} where each row represents
a SENAITE object and each column corresponds to a field.

If \code{flatten = FALSE}, returns a \code{list} of named lists containing only
the selected fields.

Returns \code{NULL} if no data is found.
}
\description{
Queries a SENAITE API endpoint and returns only the specified fields
for each object. Optionally flattens the result into a tabular format.
}
\details{
This function is a convenience wrapper around \code{get_senaite_data()}
that:
\itemize{
\item Ensures core metadata fields are always included
\item Subsets each returned object to selected fields
\item Optionally flattens nested output into a \code{data.table}
}
\subsection{Default Fields}{

The following metadata fields are always included:
\itemize{
\item \code{"title"}
\item \code{"uid"}
\item \code{"parent_path"}
\item \code{"portal_type"}
}

These are prepended to any user-supplied \code{fields} and deduplicated.
}

\subsection{Flattening Behavior}{

When \code{flatten = TRUE}, nested fields are flattened into a wide
structure. Repeated values are collapsed into a single string
(see \code{\link{flatten_senaite_output}}).
}
}
\examples{
\dontrun{
# Retrieve selected fields from analysis requests
ars <- get_senaite_fields(
  endpoint = "analysisrequest",
  fields = c("review_state", "created"),
  flatten = TRUE
)

# Retrieve raw (non-flattened) output
clients <- get_senaite_fields(
  endpoint = "client",
  fields = c("email", "phone"),
  flatten = FALSE
)
}

}
\seealso{
\code{\link{get_senaite_data}},
\code{\link{flatten_senaite_output}},
\code{\link[keyring]{key_get}}
}
